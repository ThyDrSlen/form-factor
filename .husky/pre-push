set -e

if [ "${CI_LOCAL_SKIP:-}" = "1" ]; then
  echo "⏭️  Skipping local CI checks (CI_LOCAL_SKIP=1)"
  exit 0
fi

echo "🚀 Running fast pre-push checks (policy, lint, types, tests, audit)..."
bun run ci:policy
bun run lint
bun run check:types
bun run test
bun audit --audit-level=moderate --ignore=GHSA-2g4f-4pwh-qvx6 --ignore=GHSA-rf6f-7fwh-wjgh --ignore=GHSA-3v7f-55p6-f55p --ignore=GHSA-c2c7-rcm5-vvqj
