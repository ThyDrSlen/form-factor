set -e

if [ "${CI_LOCAL_SKIP:-}" = "1" ]; then
  echo "⏭️  Skipping local CI checks (CI_LOCAL_SKIP=1)"
  exit 0
fi

echo "🚀 Running fast pre-push checks (policy, lint, types, audit)..."
bun run ci:policy
bun run lint
bun run check:types
bun audit --audit-level=moderate --ignore=GHSA-2g4f-4pwh-qvx6
